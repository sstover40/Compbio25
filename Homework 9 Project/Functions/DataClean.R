# --------------------------------------
# FUNCTION data_clean
# required packages: none
# description:
# inputs:
# outputs:
########################################
data_clean <- function(x=NULL,y=NULL){

# assign parameter defaults
if (is.null(x) | is.null(y)) {
  x <- runif(10)
  y <- runif(10)
}

# function body



return(print('...checking function: data_clean()'))

} # end of function data_clean
# --------------------------------------
# data_clean()
